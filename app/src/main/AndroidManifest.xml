<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="thezero.pkd.ams">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.wifi" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Faculty.SubmitAttendence" />
        <activity android:name=".Faculty.TakeAttendance" />
        <activity android:name=".Faculty.AttendanceList" />
        <activity android:name=".Faculty.AttendanceByDate" />
        <activity android:name=".Students.Student_attendance_Information" />
        <activity android:name=".Faculty.CourseInformatin" />
        <activity
            android:name=".SplashActivity"
            android:label=""
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Faculty.FacultyMain"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Students.Student_Main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignupForm" />
        <activity android:name=".Login_Form" />
    </application>

</manifest>